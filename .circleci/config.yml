version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: "Say hello"
          command: "echo Hello, World!"
  say-leave:
    docker:
      - image: cimg/base:stable
     steps:
      - run:
          name: "Say adios"
          command: "echo adios, People!" 

workflows:
  say-hello-workflow:
    jobs:
      - say-hello
      - say-leave